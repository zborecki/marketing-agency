'use strict';

/**
 * achievements-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::achievements-section.achievements-section');
