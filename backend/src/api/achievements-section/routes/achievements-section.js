'use strict';

/**
 * achievements-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::achievements-section.achievements-section');
